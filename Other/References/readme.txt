ref_a_field.conf

Reference a field when adding a new field. 

ref_a_array.conf

Reference a field within an array.

ref_nested_fields.conf

Reference a nested fields in a JSON object.

delete_nested_field.conf

Delete unwanted fields that are nested inside an event.

find_in_message.conf

Find the word "Test" in the generated messages. Note, it's case sensitive! (See Next)

find_in_message_insensitive.conf

Find the word "test" in the generated messages. Note, it's case INsensitive! (See Above)

find_tag.conf

Find a tag in an event.

modify_severity.conf

Parse a PHP error message, and modify a severity by the error message

